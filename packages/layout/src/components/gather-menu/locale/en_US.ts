import { GatherMenuLocale } from "../index";

const locale: GatherMenuLocale = {
  notText: 'The favorites list is empty, you can add your usual addresses to your favorites on the right.'
};

export default locale
